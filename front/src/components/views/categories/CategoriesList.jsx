import React from "react";
import { useLocation, useHistory } from "react-router-dom";

const Categories = () => {
  const location = useLocation();
  const history = useHistory();

  return (
    <div>
      <h3>CARTEGORIES LIST</h3>
      <div></div>
    </div>
  );
};

export default Categories;
